# Por defecto el globo se carga desde /animales/ (mismo servidor que `npm run dev` en la raíz).
# Ejecuta una vez: npm run build:animales
#
# Opcional — segundo servidor con HMR del globo:
# VITE_GLOBE_ORIGIN=http://127.0.0.1:5174
